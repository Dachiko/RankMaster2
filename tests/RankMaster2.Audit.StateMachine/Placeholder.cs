namespace RankMaster2.Audit.StateMachine;

// Audit findings land here as failing tests. See .claude/agents/rm2-audit-*.md:
// an auditor proves bugs and never fixes them, and a finding is a failing test, not a paragraph.
internal static class AuditScope;
