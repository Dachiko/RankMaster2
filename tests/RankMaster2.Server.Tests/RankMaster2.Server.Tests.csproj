<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <RootNamespace>RankMaster2.Server.Tests</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" Version="8.0.11" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <!-- T1c: a corpus-dependent test that returns early when tests/corpus/media is absent is
         indistinguishable from a passing one in any summary. SkippableFact/SkippableTheory make
         that a counted, printed skip instead. -->
    <PackageReference Include="Xunit.SkippableFact" Version="1.4.13" />
    <!-- openapi.yaml is the published contract and is hand-edited. Nothing else in this repository
         can tell whether it still parses; OpenApiContractTests can. -->
    <PackageReference Include="YamlDotNet" Version="16.3.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\src\RankMaster2.Server\RankMaster2.Server.csproj" />
  </ItemGroup>
</Project>
