<Project>
  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.props', '$(MSBuildThisFileDirectory)../'))" />
  <PropertyGroup>
    <!-- A-startup-and-shell.md § 1: the pc/ tree is 2.0.0, a rewrite, overriding the root's 1.1.4. -->
    <Version>2.0.0</Version>
  </PropertyGroup>
</Project>
